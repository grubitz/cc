name: build

on:
  push:
    branches: [main]
  # The calendars cover a rolling 10 years from the current year, so they need
  # rebuilding to keep gaining years. GitHub disables cron on a repo with no
  # activity for 60 days - re-enable it from the Actions tab if that happens.
  schedule:
    - cron: "0 3 1 * *"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # ember.py runs its own assertions before writing anything, so a bad
      # calculation fails the build rather than shipping wrong dates.
      - run: python ember.py
      - run: |
          mkdir -p site
          cp *.ics index.html site/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
